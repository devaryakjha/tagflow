name: tagflow_example
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  tagflow:
    path: ../../packages/tagflow

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
