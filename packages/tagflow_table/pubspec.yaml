name: tagflow_table
description: First-party table rendering extension for Tagflow's native rich content runtime.
version: 1.0.0-alpha.1
repository: https://github.com/devaryakjha/tagflow
homepage: https://docs.arya.run/tagflow
issue_tracker: https://github.com/devaryakjha/tagflow/issues

environment:
  sdk: ">=3.9.0 <4.0.0"

resolution: workspace

dependencies:
  equatable: ^2.0.7
  flutter:
    sdk: flutter
  tagflow: ^1.0.0-alpha.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  html: any

flutter:
  uses-material-design: true
