name: tagflow
description: Native rich content runtime for Flutter apps, with HTML support through a first-party adapter.
version: 1.0.0-alpha.3
repository: https://github.com/devaryakjha/tagflow
homepage: https://docs.arya.run/tagflow
issue_tracker: https://github.com/devaryakjha/tagflow/issues

environment:
  sdk: ">=3.9.0 <4.0.0"

resolution: workspace

dependencies:
  collection: ^1.19.0
  equatable: ^2.0.7
  flutter:
    sdk: flutter
  html: ^0.15.5
  meta: ^1.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
